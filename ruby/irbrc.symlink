# %w{irbtools rubygems awesome_print irb/ext/save-history mush}.each do |lib|
#   begin
#     require lib
#   rescue LoadError => err
#     $stderr.puts "Couldn't load `#{lib}`: #{err}"
#   end
# end
