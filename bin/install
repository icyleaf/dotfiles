#!/usr/bin/env bash
#
# 安装脚本里的所有 install.sh 脚本

source functions/_lib.sh

cd "$(dirname $0)"/..

run_installer() {
  find . -name install.sh | sort | while read installer ; do
    tips "-> $installer"
    sh -c "${installer}"
  done
}

run_installer


ensure_macos
setup_vscode
